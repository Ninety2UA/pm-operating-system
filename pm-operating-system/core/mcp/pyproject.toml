[project]
name = "pm-os-mcp"
version = "2.0.0"
description = "MCP server for PM Operating System — task and project management with dedup"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "pyyaml>=6.0",
]
